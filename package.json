// Stub for Cloudflare deployment — no Base44 app params needed.
export const appParams = {
  appId: 'ghostcal',
  token: null,
  functionsVersion: null,
  appBaseUrl: null,
  fromUrl: typeof window !== 'undefined' ? window.location.href : '',
};
